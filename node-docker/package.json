{
  "name": "docker_web_app",
  "version": "1.0.0",
  "description": "Node.js on Docker",
  "author": "First Last <first.last@example.com>",
  "main": "server.js",
  "scripts": {
    "dev": "docker-compose -f docker-compose-dev.yml up --build",
    "build": "docker-compose -f docker-compose-prd.yml up --build"
  },
  "dependencies": {
    "express": "^4.18.2"
  }
}